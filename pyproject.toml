# python -m pip install -e .

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "big-tool"
dynamic = ["version"]
dependencies = ["pillow>=10.0", "tkinterdnd2>=0.4"]

[project.optional-dependencies]
build = ["nuitka[onefile]>=2.0"]

[project.scripts]
big-tool = "big_tool.cli:main"
big-tool-app = "big_tool.app.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "big_tool.version.__version__"}

[tool.pytest.ini_options]
addopts = "-v --tb=short --show-capture=no"
testpaths = ["tests"]
pythonpath = ["src"]
